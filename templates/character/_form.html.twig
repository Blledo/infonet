{{ form_start(form, {attr: attr|default({})}) }}
    {{ form_widget(form) }}

    <button type="submit" class="{{ button_css|default("btn btn-primary") }}">
        Save
    </button>

    {% if include_back_to_home_link|default(false) %}
        <a href="{{ path('character_list') }}" class="btn btn-link">
            Back
        </a>
    {% endif %}
{{ form_end(form) }}
